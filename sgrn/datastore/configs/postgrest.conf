db-uri = "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@127.0.0.1:5432/${POSTGRES_DB}"
db-schema = "postgrest"
db-anon-role = "${POSTGRES_USER}"
server-host = "127.0.0.1"
server-port = 3000
jwt-secret = "${JWT_SECRET}"
