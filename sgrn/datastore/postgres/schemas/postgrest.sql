-- ============================================================
-- schema: postgrest
-- ============================================================
-- This schema exposes views to PostgREST for the Admin dashboard.
-- View definitions live in postgrest.sql (root) which runs after
-- all base schemas and roles are established.
-- ============================================================
create schema if not exists postgrest;
